const core = require('./src/core');
const util = require('./src/util');
const field = require('./src/field');
const record = require('./src/record');
const recordType = require('./src/recordType');
const trait = require('./src/trait');

module.exports = {
  core,
  util,
  field,
  record,
  recordType,
  trait,
};
